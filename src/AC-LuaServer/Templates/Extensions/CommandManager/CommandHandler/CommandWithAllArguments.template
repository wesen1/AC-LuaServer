{#
 # Prints a command including all of its arguments.
 #
 # @tparam string[] colors The list of colors
 # @tparam BaseCommand command The command
 #}

{(lua/config/templates/TextTemplate/CommandHandler/Command.template,
  { colors = colors, command = command, arguments = command:getArguments() }
)}
